services:
  - type: web
    name: student-ai-hub
    runtime: static
    buildCommand: "" 
    publishPath: "./"
    headers:
      - path: /*
        headers:
          - key: Cross-Origin-Embedder-Policy
            value: require-corp
          - key: Cross-Origin-Opener-Policy
            value: same-origin
